spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spacex_dashboard
    username: spacex
    password: secret123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  cache:
    type: none

logging:
  level:
    org.springframework: DEBUG
    com.spacex.launcher: DEBUG
