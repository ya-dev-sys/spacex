spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spacex_dashboard
    username: spacex
    password: secret123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379
